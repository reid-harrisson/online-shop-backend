#The application host name
http_config:
  host: localhost
  port: 8014

#Parameters for getting the access to the database
db_config:
  user: local_user
  password: password
  driver: mysql
  name: test
  host: localhost
  port: 3306
